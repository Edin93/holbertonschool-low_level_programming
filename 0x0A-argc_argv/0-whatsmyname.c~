#include "holberton.h"
/**
 * main - print program name
 * @argc: arguments count.
 * @argv: arguments vector.
 * Return: int.
 */
int main(int argc, char *argv[])
{
	int len = 0;

	(void)argc;
	while ((argv[0])[len] != '\n')
	{
		_putchar((argv[0])[len]);
		len++;
	}
	return (0);
}
